{
  "name": "yt-audio-only",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "web-ext build --source-dir src --overwrite-dest",
    "lint": "web-ext lint --source-dir src"
  },
  "devDependencies": {
    "web-ext": "^10.3.0"
  }
}